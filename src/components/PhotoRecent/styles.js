import { makeStyles } from "@material-ui/core";


export const useStyle = makeStyles(() => ({
    cardImage: {
        height: '100%',
        width: '100%'
    },
    cardText: {
        fontSize: 14,
        fontWeight: '300',
        margin: 0,
        marginRight: 5,
    }
}))