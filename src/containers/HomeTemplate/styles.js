import { makeStyles } from "@material-ui/core";


const useStyle = makeStyles(() => ({
    wrapper: {
        display: 'flex',
        flexDirection: 'row',
        alignItems: 'stretch',
    }
}))
export default useStyle;